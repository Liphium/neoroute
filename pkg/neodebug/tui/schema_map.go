package tui

// Works like this:
// - Some key combination for switching between key and value, the state that you were editing keys / values persists
// - One input for every key, rest handled by the selection nodes obv
