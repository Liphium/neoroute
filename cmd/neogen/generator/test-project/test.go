package testproject

// TODO: Create an actual test project that contains every type of transporter + every type of route for every transporter. This can then be used in tests to actually verify the generator does proper generation and doesn't fail.
